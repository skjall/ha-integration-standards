[pytest]
asyncio_mode = auto
testpaths = tests
# ha-coverage enforces the per-module floors a single number hides; only what
# always applies lives here.
addopts =
    --cov=custom_components.{{domain}}
    --cov-report=term-missing
    --cov-fail-under=95
